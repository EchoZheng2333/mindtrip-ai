# 2026-08-13 工作日志

## 借鉴「智能旅行助手」+ 演示版第一批改动（全部完成并推送）

### 借鉴评估（echo 要求读 hello-agents 第十三章）
- 读完全文（1581 行）：Vue3+FastAPI+4 智能体+MCP 高德/Unsplash 的完整旅行规划项目
- 借鉴分档：🟢 动线地图/预算明细/归途卡导出（进演示版）；🟡 天气/多智能体（演示版做天气降级版，多智能体赛后）；🔴 SPA 架构/Pydantic（不碰，与 ADR 冲突）
- echo 确认：四项全做，地图用高德 JS API（发现 v1.2 已有实现），先做测评页

### 第一批改动（commit daef124，已验证推送）
1. **quiz.ejs 全视觉沉浸**：氛围光晕层（--aura-hue 色温随题切换，8 快测暖金→3 约束冷青）+ Web Audio 合成选择音效（无音频文件，五声音阶叮/段落低音/提交和弦）+ 选项涟漪；全部 try/catch 降级；知情同意折叠提示
2. **index.ejs**：CTA 直通 /quiz；final 区知情同意折叠（合规保留，不再单开一屏）
3. **profile.ejs**：人格名逐字浮现 + 指数圆环辉光（reduce-motion 降级）
4. **return.ejs 重写**：心灵足迹纪念卡——人格名/美学DNA/指数对比/行程回顾时间线/AI札记 + html2canvas 导出 PNG（本地 /vendor 托管，去 Web3）
5. **route.ejs**：人均预算明细卡（类型×价格档：景点30/60/120、餐饮40/80/150、手作60/120/300、夜间30/60/100 + 交通20/天）+ 当日天气卡
6. **api.js**：/return 加 checkin_places + aesthetic_dna；新增 /weather 高德天气代理（降级友好）
7. **依赖**：html2canvas ^1.4.1

### 验证结果（全通过）
- 页面全 200；完整流程：11 题测评→山间独行者(85)→动线→打卡2场景→归途(97, +12)，DeepSeek 诗意总结真实生成（llm_generated: true）
- 天气接口降级正常（旧 key 非 Web 服务类型 → ok:false，前端隐藏）

### 注意
- 天气功能需要**Web 服务类型**的高德 key 才会生效（现有 JS API key 会静默降级）
- 归途卡导出用 html2canvas 本地化，不依赖 CDN

### 待办
- 演示版第二批：场景数据精修（精选 6-8 个配图/文案）、5 分钟演示脚本
- echo 尚未轮换 DeepSeek/高德 key（密钥事件待办）

## 高德双 key 支持（echo 声称已切换 Web 服务类型）
- 实测：.env 中 AMAP_KEY(7dcf...) 调用天气接口仍报 USERKEY_PLAT_NOMATCH(10009)——切换未生效或该 key 仍是 JS API 类型
- 关键规则：高德一个 Key 只能绑定一个平台类型——地图(Web端 JS API)与天气/POI(Web服务)需**两个 key**
- 代码已改：/api/v1/weather 优先用 `AMAP_WEB_KEY`，无则回退 `AMAP_KEY`（降级友好）；.env.example 增加 AMAP_WEB_KEY 占位符说明
- echo 待办：控制台确认 key 类型；配置 .env：AMAP_KEY=JS API key（地图）、AMAP_WEB_KEY=Web服务 key（天气）

## ✅ 高德/DeepSeek key 全部就绪（2026-08-13 10:45 验证通过）
- echo 之前在**另一个工作区**改的 .env，现已在当前工作区确认：
  - AMAP_WEB_KEY = f8c8...b086（Web 服务类型）→ 天气接口实测 `{"ok":true, 多云 31°C 北风≤3 湿度73%}` ✓
  - AMAP_KEY = 7dcf...8373（JS API，地图用）✓
  - DEEPSEEK_API_KEY = sk-9...a034（新 key，LLM 已启用）✓ —— **DeepSeek 轮换也一并完成了**
- 服务端 /api/v1/weather 返回 ok:true，动线页天气卡片将点亮
- 密钥事件闭环：历史已清除 + 双 key 已轮换

## 第二批：场景配图 + 演示脚本（commit f0ac432）
- **8 张 AI 生成场景配图**（ImageGen，每张约 5-10 credits，共约 40-80 已消耗）：御窑/拉坯/柴窑/陶溪川/三宝瓷谷/茶室/农家小厨/抚州弄
  - 转 JPG 压缩（单张 137-299KB，共 1.7MB），存 `public/img/scenes/{id}.jpg`
  - scenes.js 8 场景加 image 字段；scene.ejs hero 主图（无图隐藏）；route.ejs 动线卡片缩略图；/api/v1/scenes 返回 image
  - 验证：图片 200、接口带 image、场景页 200
- **演示脚本**：docs/demo-script.md（5 分钟 6 屏节奏话术 + 评委 Q&A 7 问 + 风险预案）；Obsidian 心旅AI/02-演示脚本.md 同步
- ⚠️ push 遇 SSL 网络抖动（github 443），待重试
- 未配图场景：changjiang_riverside、taoxichuan_night_market（备选池，后续可补）

## 第二批扫尾：10 场景全部配图（commit 90c2e1c）
- 补生成昌江落日、陶溪川夜市 2 张（同风格，约 10-20 credits），转 JPG 后 10 场景全部有图
- 验证：scenes 接口 10/10 带 image，新图 200，已推送 GitHub
- Unsplash 决策：不接（AI 配图已覆盖且风格统一；Unplash 国外服务搜索本地小众场景命中率低、网络不稳）

## 修复：测评页题目不显示（commit bfdb347）
- 现象：echo 反馈测评页出现大片代码、看不到题目
- 根因：插入音频/色温代码块时误加了多余 `</script>`，后续全部 JS（loadQuiz/renderQuestion 等）被挤出 script 块，浏览器当文本显示
- 修复：删除多余闭合标签，脚本恢复 270 行、node --check 语法通过；其余页面 script 标签配对检查全部 ✓
- 教训：EJS 里插入 JS 块后必须检查 `<script>`/`</script>` 配对数 + 用 node --check 验证

## 修复：场景页返回动线丢城市参数（commit 1c53d10）
- 现象：echo 反馈从场景详情页点"返回动线"会跳回选城页（中国地图），要重选城市
- 根因：/route 无 city 参数时 pages.js 兜底重定向 /explore；scene 页返回动线用的是裸 /route
- 修复：scene.ejs 底部"返回动线"/"继续心旅"按钮 + leaveToRoute() 均拼接 ?city=scene.city（接口已确认返回 city 字段）
- 顺带：这是 v1.2 既有 bug（非今日引入），修复后任何入口进 scene 再返回动线都正常

## 修复：动线页首屏加载慢（commit 3d9adee）
- 现象：echo 反馈每次进动线页都要等很久；希望用上已有的等待过渡页
- 根因：/api/v1/route/main 同步 await generateWhyFitBatch（DeepSeek 12s 超时上限），阻塞首屏；且生成文案未在动线卡片展示（白等）
- 修复：route/:type 移除 LLM 调用（首屏 12s→2ms）；新增 /api/v1/route/why-fit 异步接口（session 级缓存，同人格+动线只调一次 LLM）；route.ejs 加加载过渡层（复用等待页样式）+ 卡片展示"为什么适合你"（静态 why_fit 兜底 + LLM 15s 超时渐进更新）
- 路由坑：/route/why-fit 会被 /route/:type 抢占，需在 :type handler 首行 next() 让位
- 验证：route/main 0.005s；why-fit 首次 4.2s（真实 LLM 文案）/二次 3ms 缓存命中

## 下午 UI 优化轮（17:00-18:00）
- 时间轴 UI 三轮迭代：① 时间标签移左侧列（padding 120px）→ ② 去 emoji+垂直居中 → ③ echo 澄清要"时间轴整体左移，竖线对齐预算卡左边缘"，恢复 padding 32px、时间标签回卡片顶部（commit 9ffbfc6 / 9bc6df3）
- 卡片信息三层重排：主信息(名称+类型+时限) → 实用信息(营业/人流/预算) → 深度文案(诗意+why_fit)，操作归底（.tl-head/.tl-meta/.tl-actions）
- 动线页提速（commit 3d9adee）：route/main 去 LLM 阻塞（12s→2ms），why-fit 异步接口 + session 缓存（首次4s/二次3ms），加载过渡层
- 打卡状态可视化（commit fb0d328）：/scenes 返回 checked_in；四件套=金色徽标+卡片变淡+缩略图✓角标+「已打卡 x/y」进度胶囊；备选池同步标注
- 高德 jscode 支持（commit 9c21fd2）：AMAP_JSCODE 环境变量（2021后 key 必须配安全密钥否则地图降级 SVG）
- ⚠️ 代理坑：git 全局 proxy 7890 未运行，push 用 `git -c http.proxy=http://127.0.0.1:54510 push` 解决（已记 MEMORY.md）
