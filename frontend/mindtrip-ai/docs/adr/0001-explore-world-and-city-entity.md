# 探索世界页与城市实体化

在画像页与城市路线页之间新增「探索世界页」（地图选城），并把「城市」提升为一等实体：新建 `data/cities.js`（`jingdezhen` open + 苏州/杭州/大理/成都 closed 占位），`scenes.js`/`routes.js` 增加 `city` 归属字段，`/route` 按 `?city=` 参数化，无参数时跳转 `/explore`。选这条路的权衡：单城硬编码最快，但跨城复制（商业计划书「3–5 个人文城市」）是明确方向，城市实体化越晚改造成本越高；占位城市只做地图光点，不预置场景数据，避免空转。
